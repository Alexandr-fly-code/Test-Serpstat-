export const moreInfoPost = (dataComments,dataUsers, dataPosts, userId, id, newMass) => ({
    type: 'INFO_POST',
    dataComments,
    dataUsers,
    dataPosts,
    userId,
    id,
    newMass,
});

